name: Update README

on:
  workflow_dispatch:
  schedule:
    - cron: '10 17 * * *'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Pull latest changes
        run: git pull origin main
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Generate README
        run: node script/generate-readme.js
      
      - name: Check for changes
        id: check
        run: |
          if git diff --quiet README.md; then
            echo "CHANGED=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No changes to README"
          else
            echo "CHANGED=true" >> $GITHUB_OUTPUT
            echo "✅ README has changes"
          fi
      
      - name: Commit and push
        if: steps.check.outputs.CHANGED == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          FILES_COUNT=$(ls -1 frontend/static/history/*.json 2>/dev/null | wc -l)
          git commit -m "📝 Update README with history archive ($FILES_COUNT files) [skip ci]"
          git push
      
      - name: Summary
        run: |
          echo "### 📝 README Update" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ${{ steps.check.outputs.CHANGED == 'true' && 'Updated' || 'No changes' }}" >> $GITHUB_STEP_SUMMARY
